<p align="center">
  <img src="https://github.com/user-attachments/assets/eda11651-d644-4067-a973-1513336960b0"
       alt="ZRC-Logo"
       width="500"></p>

---

<div align="center"><h1> ⚙️ Modules Showcase ⚙️ </h1>
  
<details> <summary><strong> [Click to Expand] </strong></summary>

| Module | Description |
|--------|-------------|
| **Better Less** <br><sub>`modules/better-less.zsh`</sub> | Enhanced wrappers around `less` with color-preserving helpers. |
| **List Recent Files** <br><sub>`modules/list-recent-files.zsh`</sub> | Trimmed, color-safe view of recently modified files. |
| **Extract Archives** <br><sub>`modules/extract-archives.zsh`</sub> | One command to unpack many common archive formats. |
| **Silent nohup** <br><sub>`modules/silent-nohup.zsh`</sub> | Background commands without generating `nohup.out`. |
| **SSH Destination** <br><sub>`modules/ssh-destinations.zsh`</sub> | Export a reusable remote target for `ssh`. |
| **Current Shell RC** <br><sub>`modules/current-shell-rc.zsh`</sub> | Detect the active shell and expose its configuration file. |
| **Reload RC** <br><sub>`modules/reload-rc.zsh`</sub> | Wrapper to re-source the detected shell rc file. |
| **Unsource** <br><sub>`modules/unsource.zsh`</sub> | Remove functions, aliases, and variables defined in another file. |
| **Tidy Media** <br><sub>`modules/tidy-media.zsh`</sub> | Move common media types into organized directories. |
| **Copy & Paste** <br><sub>`modules/copy-paste.zsh`</sub> | Clipboard helpers for piping and retrieving text. |
| **Copy File Contents** <br><sub>`modules/copy-file-contents.zsh`</sub> | Copy the contents of a file directly to the clipboard. |
| **Quick Notes** <br><sub>`modules/quick-notes.zsh`</sub> | Append and review notes in `~/quick_notes.txt`. |
| **Prompt EOL** <br><sub>`modules/prompt-eol.zsh`</sub> | Hide the default prompt end-of-line marker. |
| **Disk Space** <br><sub>`modules/disk-usage-alias.zsh`</sub> | Alias for sorted directory size listings. |
| **Weather** <br><sub>`modules/weather.zsh`</sub> | Fetch concise weather reports via `wttr.in`. |
| **Find and Open** <br><sub>`modules/find-and-open.zsh`</sub> | Locate files with `fzf` and open them via `xdg-open`. |
| **Find Files** <br><sub>`modules/find-files-alias.zsh`</sub> | Case-insensitive file lookup helper. |
| **Find Directories** <br><sub>`modules/find-directories-alias.zsh`</sub> | Case-insensitive directory lookup helper. |
| **Command Timer** <br><sub>`modules/command-timer.zsh`</sub> | Measure runtime for arbitrary commands. |
| **Python HTTP Server** <br><sub>`modules/python-http-server.zsh`</sub> | Alias for the builtin Python simple HTTP server. |
| **Lazy CD** <br><sub>`modules/lazy-cd.zsh`</sub> | Short aliases for jumping up the directory tree. |
| **mkcd** <br><sub>`modules/mkcd.zsh`</sub> | Create a directory and enter it in one step. |
| **Colorized Man** <br><sub>`modules/colorized-man.zsh`</sub> | Color configuration for `man` pages. |
| **Grep+** <br><sub>`modules/grep-plus.zsh`</sub> | Color and count variants of `grep`. |
| **NET-PACK** <br><sub>`modules/net-pack.zsh`</sub> | Collection of networking aliases and helpers. |
| **CLI Clock** <br><sub>`modules/cli-clock.zsh`</sub> | Render a large ASCII clock in the terminal. |
| **ASCII Fonts** <br><sub>`modules/ascii-fonts.zsh`</sub> | Preview installed `figlet`/`toilet` fonts with optional delays. |
| **UnWebP** <br><sub>`modules/unwebp.zsh`</sub> | Convert WebP files to PNG and remove the originals. |


</details>
</div>

---


